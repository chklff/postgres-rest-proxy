{
  "name": "postgres-rest-proxy",
  "version": "1.0.0",
  "description": "Proxy to perform REST API call to PostgreSQL",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "postgres-rest-proxy"
  },
  "author": "Alex Chekalov",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  }
}